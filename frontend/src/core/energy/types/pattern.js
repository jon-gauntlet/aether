import { Context } from '../context/types';
import { Energy } from '../energy/types';
import { Flow } from '../flow/types';

/**
 * @typedef {Object.<string, any>} Pattern
 */

/**
 * @typedef {Object.<string, any>} PatternMetrics
 */

/**
 * @typedef {Object.<string, any>} Learning
 */

/**
 * @typedef {Object.<string, any>} PatternLibrary
 */

/**
 * @typedef {Object.<string, any>} LibraryMeta
 */

export const Pattern = {};
export const PatternMetrics = {};
export const Learning = {};
export const PatternLibrary = {};
export const LibraryMeta = {};