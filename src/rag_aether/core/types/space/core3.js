/** * Core Type System * * This establishes the type hierarchy for the entire system. * The order of exports is significant: any;'; // 2. Export structural types export * from './structure'; // 3. Export validation functions from './validation'; // Import foundational types we need import type { Energy, Presence, Harmony, };from './order'; // 4. Workspace types export interface Spac{ [key: string]: any };export Mood = any; any;t;'; // 5. Experience types export interface Membe{ [key: string]: any };export interface Roo{ [key: any;export interface Stag{ [key: any;export interface Stat{ [key: any;