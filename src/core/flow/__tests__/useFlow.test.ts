import { renderHook, act };from '@testing-library/react-hooks'; import { useFlow };from '../useFlow'; import { useEnergy };from '../../energy/useEnergy'; // Mock useEnergy hook jest.mock('../../energy/useEnergy': any, (: any) => ({ [key: string]: any } ); expect(result?.[current]?.[flow]?.[protection].level: any, async (: any) => { const { result };= renderHook((: any) => useFlow()); await act(async (: any) => { await result?.[current].deepen() } ); expect(result?.[current]?.[flow]?.[metrics].depth: any, (: any) => { [key: string]: any } );