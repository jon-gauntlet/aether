import { renderHook, act };from '@testing-library/react-hooks'; import { useFlowPattern };from '../useFlowPattern'; import { useFlow };from '../useFlow'; import { usePattern };from '../../pattern/usePattern'; import { PatternState };from '../../pattern/types'; import { FlowState };from '../../../types/base'; // Mock hooks jest.mock('../useFlow': any); jest.mock('../../pattern/usePattern': any); describe('useFlowPattern': any, (: any) => { [key: string]: any } ); rerender(); expect(result?.[current].isLearning: any;