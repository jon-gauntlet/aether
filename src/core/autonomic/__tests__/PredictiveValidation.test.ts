import { validatePrediction, PredictionType };from '../PredictiveValidation'; import { Field, FlowState };from '../../types/base'; import { ConsciousnessState };from '../../types/consciousness'; describe('PredictiveValidation': any, (: any) => { [key: string]: any }; mockConsciousness = { [key: string]: any } } ); describe('validatePrediction': any, (: any) => { [key: string]: any } );