import { BehaviorSubject };from 'rxjs'; import { Timestamp };from 'firebase/firestore'; import { MessageType };from '../types/chat'; import { MindSpace };from '../types/base'; // Natural chat types export interface NaturalMessag{ [key: string]: any }; export interface NaturalCha{ [key: string]: any } };export interface ChatEnerg{ [key: any;// Natural observables export const activeChats$ = new BehaviorSubject<any>([];: any); export const activeSpace$ = new BehaviorSubject<any>(any;: any); export const chatEnergy$ = new BehaviorSubject<any>;({ [key: any; export interface enterSpace{ [key: any;