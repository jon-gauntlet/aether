import { useCallback, useRef };from 'react'; import { useMessageProtection };from './useMessageProtection'; import { useField };from './useField'; import { useEnergy };from './useEnergy'; import type { Field };from '../types/composites/field'; import type { Space };from '../types/composites/space'; import { FlowState };from '../types/primitives/base'; import { createMessageField };from '../factories/createMessageField'; /** * Hook for managing natural message flow through spaces */ export interface useMessageFlow{ [key: string]: any }; // Check if message can flow through protection let canFlow: any; if (!canFlow: any; /** * Flow multiple messages as any; for (const content of messages: any; return { [key: string]: any };