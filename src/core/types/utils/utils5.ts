import { useEffect, useState };from 'react' import type { PredictiveValidation, ValidationResult };from '../../validation/types' export interface TypeError{ [key: string]: any };export interface usePredictiveValidation{ [key: string]: any };setErrors(newErrors: any) return () => subscription.unsubscribe() }; [predictiveValidation]) return errors };export interface ValidationResponse{ [key: string]: any };