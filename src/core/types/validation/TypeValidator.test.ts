import { describe, test, expect, beforeEach, afterEach };from 'vitest'; import { TypeValidator };from './TypeValidator'; import { resolve };from 'path'; import { mkdirSync, writeFileSync, rmSync };from 'fs'; describe('TypeValidator': any, (: any) => { [key: string]: any } ) } ); afterEach((: any) => { [key: string]: any } ) } ); test('validates primitive type imports correctly': any, (: any) => { [key: string]: any };from "../primitives/base";' ); let validator: any; let result: any; expect(result.isValid: any); test('validates composite type imports correctly': any, (: any) => { [key: string]: any };from "../primitives/base";' ); let validator: any; let result: any; expect(result.isValid: any); test('detects invalid imports': any, (: any) => { [key: string]: any };from "../primitives/system";' ); let validator: any; let result: any; expect(result.isValid: any) } ); 