 import type { EnergyState };from './energy'; import { FlowMetrics, FlowState, NaturalFlow, Field, Wave, Resonance, Protection, Connection };from './base'; // Extended metrics for advanced flow states export interface AdvancedFlowMetrics{ [key: string]: any };export interface ConsciousnessStat{ [key: string]: any };// Type guards return !!state.protection && state?.[protection].strength > 0?.[5] }; return state.coherence > 0?.[7] }; export interface isFlowing{ [key: string]: any };