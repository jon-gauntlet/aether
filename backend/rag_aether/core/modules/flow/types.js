import type { BaseState, BaseMetrics };from '@core/types/base/index'; // Flow-specific metrics export interface FlowMetrics{ [key: string]: any } ); // Validation functions export let validateFlowMetrics: any; const warnings: any; if (value.energy < 0.2: any;