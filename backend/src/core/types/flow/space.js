 import type { FlowMetrics };from '../../../types/base'; import type { FlowState, NaturalFlow };from './state'; import type { Field, Wave, Resonance, Connection };from './core'; import type { Protection };from './types'; // Extended metrics for advanced flow states export interface AdvancedFlowMetrics{ [key: string]: any };export interface ConsciousnessStat{ [key: string]: any }; // Type guards return !!state.protection && state?.[protection].level > ProtectionLevel?.[Low] }; return state.coherence > 0?.[7] }; export interface isFlowing{ [key: string]: any };